# Sub-Agent Report Trace

- **Agent**: Explore
- **Session**: /MTg0OWVkM2MtYTgxYS00YmRjLTk3ODYtZjE4NGZmMTRjNDJi
- **Raw report chars**: 18609
- **Compressed (delivered to parent) chars**: 19417
- **Distinct files mentioned in raw report**: 23

## Spawn Prompt (what the parent asked)

Map the exact field names and types of these objects in the DeployIQ repo at c:/Users/risha/Desktop/deployIQ. I am writing report/assemble.py and must consume these objects. Report EXACT attribute names and types from the source files — do not guess.

1. `solution/schema.py` → `SolutionEstimate` full field list, and these sub-objects: `TaskAutomationEstimate` (task, capability, estimate, workload_share, workload_share_provenance, hitl, architecture, benchmark_basis, benchmark_anchor), `PerformanceMetric` (metric, estimate), the object behind `reference_comparison` (match, deviation_reason, active_deviations, unevaluated_conditions), the object behind `risk`/`risk_controls`, `compliance_verdicts`, `compliance_exclusions`, `compliance_statement`, `compliance_gap`, `key_uncertainties`, `provenance_warnings`, `fit_explanations`, `needs_more_information`, `confidence_notes`, `recommended_pattern`, `recommended_implementation`, `integration_complexity`, `integration_basis`, `engineering_effort`, `effort_basis`, `engineering_hours`, `overall_automation`, `task_automation`.

2. `solution/schema.py` → `AlternativesResult` (alternatives, ordering_basis, statement, is_recommendation, economics_included, categories_not_in_registry, rejected, llm_guard_notes) and the sub-objects `Alternative` (id, name, difference_from_primary, difference_kind, comparison with approach, strengths, limitations, when_preferable, implementation_complexity, implementation_complexity_basis, expected_automation, explanation) and `rejected` entry (pattern_id, implementation_id, reason).

3. `solution/schema.py` → the risk-related types: `RiskInputs`, risk score output, `risk_controls` category+control, compliance verdict types.

4. The labor realization type `calc.ai_state.LaborRealization` enum values, and `SolutionEstimate` / `EconomicResult` fields referencing realization policy (e.g. realization_statement, freed capacity).

5. `schemas/assessment_state.py` → `AssessmentState` field list (problem, process, sector, monthly_volume, avg_time_per_unit_minutes, current_headcount, fraction_time_on_process, fully_loaded_annual_cost, geography, worker_role, worker_role_canonical, current_tools, current_quality_metric, current_quality_value, required_accuracy, data_readiness, integration_complexity, risk, get_value(), get_tag(), currency, field_resolution) and `Provenance`, `RangeEstimate`, `Sector` enum values, `DataReadiness`, `EffortBand`, `ImpactSeverity`.

6. `calc/economic_score.py` or wherever the economic/feasibility/risk scores are defined — the `Score` object shape: label, value, band, computable, missing_inputs, flags, bounds_type, inputs_held_fixed, presentable_as_strong, sanity. And `ScoreBundle` fields (economic, feasibility, risk).

Report the EXACT attribute names as they appear in code — quote the relevant dataclass/pydantic field blocks. This is critical for correct assembly.

## Raw Report (what the sub-agent produced, pre-compression)

I'll map these objects precisely. Let me start by locating the relevant files and reading their schemas.Let me read the three key files: solution/schema.py, schemas/assessment_state.py, and calc/ai_state.py.Now let me examine the economic/feasibility/risk score files and find the `Score`, `ScoreBundle`, and `EconomicResult` definitions.Now I need `ScoreBundle`, `EconomicResult`, and `FirstYearEconomics`. Let me locate them.Let me read the `ScoreBundle`, `EconomicResult`, and `FirstYearEconomics` definitions, plus the engine's realization_statement context.Let me check how `risk_controls`, `compliance_verdicts`, and `compliance_exclusions` dicts are structured, plus the `LaborConsistency` referenced in EconomicResult.Let me read the exact dict shapes for `risk_controls`, `compliance_verdicts`/`compliance_exclusions` construction, and `LaborConsistency`.Let me find the `RiskControl`, `ComplianceVerdict`, and `ComplianceExclusion` class shapes.Let me read `RiskControl`, `ComplianceVerdict`, and search for the compliance exclusion object.Let me read the `ExcludedCandidate`, `ComponentVerdict`, and `ClaimStatus` shapes to fully describe the exclusion/verdict dict structure.The dedup blocked reads of different line ranges. Let me re-read the specific ranges I need.## Exploration Report

**Confidence**: HIGH - I read the three main schema files in full and all score/economic/labor definition sites. Partial gap: none critical.

**Files Examined**: solution/schema.py (full, 373 lines), schemas/assessment_state.py (full, 427 lines), calc/ai_state.py (full, 287 lines), calc/models.py (full 253 lines), calc/economic_score.py, calc/feasibility_score.py, calc/risk_score.py, calc/lifecycle.py (lines 1-120), calc/engine.py (lines 60-130), calc/driver_ranking.py (lines 80-130), calc/labor.py (lines 90-120), solution/estimator.py (lines 360-425), solution/ranking.py (lines 90-130, 237-250), solution/risks.py (lines 25-60), lib/compliance.py (lines 160-215).

**Key Tool Path**: listDirectory → readFile (schemas) → search (ScoreBundle/EconomicResult/FirstYearEconomics) → targeted reads of dict-shape constructors.

### Findings

- `schemas/assessment_state.py:273-347` — **AssessmentState** exact fields: `sector: Sector` (required, no default), `problem: str = ""`, `geography: Optional[str] = None`, `process: Optional[str] = None`, `monthly_volume: Optional[RangeEstimate] = None`, `avg_time_per_unit_minutes: Optional[RangeEstimate] = None`, `current_headcount: Optional[RangeEstimate] = None`, `worker_role: Optional[str] = None` (raw user words), `worker_role_canonical: Optional[ProcessRole] = None`, `fully_loaded_annual_cost: Optional[RangeEstimate] = None`, `fraction_time_on_process: Optional[RangeEstimate] = None` (0-1). Other current-cost components (all Optional[RangeEstimate]): `annual_tooling_cost`, `monthly_tooling_cost`, `error_rate` (fraction 0-1), `rework_time_per_error_minutes`, `annual_rework_cost`, `annual_other_direct_cost`, plus `other_direct_cost_description: Optional[str]`. Quality: `current_quality_metric: Optional[CurrentQualityMetric]`, `current_quality_value: Optional[RangeEstimate]`. Feasibility: `required_accuracy: Optional[RangeEstimate]` (0-1), `existing_data: Optional[str]`, `data_readiness: Optional[DataReadiness]`, `current_tools: list[str] = []`, `integration_complexity: Optional[EffortBand]`. `risk: RiskInputs = default_factory`. `process_stages: list[ProcessStage] = []`. Bookkeeping: `provenance: dict[str, Provenance] = {}`, `turn_count: int = 0`, `complete: bool = False`, `status: InterviewStatus = INTERVIEWING`, `field_resolution: dict[str, FieldMeta] = {}`. Methods: `currency` is a PROPERTY (not field) returning Optional[str] derived from `GEOGRAPHY_CURRENCY` via geography (e.g. india/in→INR, us/usa/united states→USD); else None. `get_value(field)` resolves dotted paths (reads nested via getattr); `get_tag(field)` returns `self.provenance.get(field)`; `set_value(field, value, provenance)` writes dotted paths and coerces scalars/dicts into RangeEstimate for range-typed fields. Note: `field_resolution` is a dict of `FieldMeta`, and field are NOT flat — `current_tools`, `process_stages` etc. are list fields. `model_config = {"validate_assignment": True}`.

- `schemas/assessment_state.py:16-31` — **Provenance** enum values: USER_PROVIDED="user_provided", SOURCED="sourced", ESTIMATED="estimated", ASSUMED="assumed", DERIVED="derived".

- `schemas/assessment_state.py:34-36` — **Sector** enum values: CUSTOMER_SUPPORT="customer_support", DOCUMENT_PROCESSING="document_processing" (only those two).

- `schemas/assessment_state.py:52-69` — **RangeEstimate**: `min: float`, `max: float`, `confidence: Literal["low","medium","high"] = "medium"`, `provenance: Provenance = ESTIMATED`, `source: str = ""` (rendered citation), `source_id: Optional[str] = None` (stable evidence id).

- `schemas/assessment_state.py:72-84` — **DataReadiness** enum: NONE="none", MINIMAL="minimal", PARTIAL="partial", GOOD="good", EXCELLENT="excellent". `:86-93` — **ImpactSeverity** enum: NEGLIGIBLE, MINOR, MODERATE, MAJOR, SEVERE. `:166-169` — **EffortBand** enum: SMALL="small", MEDIUM="medium", LARGE="large".

- `schemas/assessment_state.py:95-115` — **ProcessRole** enum: CUSTOMER_SUPPORT_AGENT, CUSTOMER_SUPPORT_SPECIALIST, ACCOUNTS_PAYABLE_CLERK. `:118-133` — CurrentQualityMetric enum (FIRST_CONTACT_RESOLUTION, ESCALATION_RATE, REWORK_RATE, EXCEPTION_RATE, FIRST_PASS_YIELD, STRAIGHT_THROUGH_RATE).

- `schemas/assessment_state.py:203-225` — **RiskInputs** (`model_config = {"validate_assignment": True}`): `failure_impact: Optional[str] = None` (free text), `failure_impact_severity: Optional[ImpactSeverity] = None`, `compliance_exposure: Optional[list[str]] = None` (canonical keys), `compliance_requirements: list[ComplianceRequirement] = []`. `failure_probability` and `reliability_gap` are NOT fields — derived elsewhere.

- `schemas/assessment_state.py:151-163` — **ComplianceRequirement**: `standard: str`, `stated_as: str = ""`, `hard_requirement: bool = True`.

- `schemas/assessment_state.py:186-200` — **FieldResolution** enum (MISSING, RESOLVED, AMBIGUOUS, LOW_CONFIDENCE, CONTRADICTORY, NEEDS_DETAIL) and **FieldMeta**: `status: FieldResolution = MISSING`, `attempts: int = 0`, `reason: Optional[str] = None`.

- `solution/schema.py:171-183` — **TaskAutomationEstimate**: `task: str`, `capability: Capability` (enum, required), `architecture: str`, `benchmark_basis: str` (LLM prose only), `estimate: RangeEstimate`, `hitl: HitlMode = AI_ASSISTED`, `workload_share: float = 0.0` (derived by calc, never LLM-supplied), `workload_share_provenance: Provenance = DERIVED`, `handling_time_minutes: Optional[RangeEstimate] = None`, `benchmark_anchor: Optional[str] = None`, `divergence_note: str = ""`.

- `solution/schema.py:38-44` — **HitlMode** enum: AUTONOMOUS="autonomous", AI_ASSISTED="ai_assisted", HUMAN_REVIEW="human_review", HUMAN_ONLY="human_only", ESCALATION="escalation".

- `solution/schema.py:26-30` — **PerformanceMetric**: `metric: str`, `estimate: RangeEstimate`.

- `solution/schema.py:186-202` — **ReferenceComparison**: `reference_id: str`, `expected_pattern: str`, `selected_pattern: str`, `match: bool`, `alignment: float` (0-1, entered ranking), `deviation_reason: str = ""`, `active_deviations: list[str] = []` (conditions that fired, in reference's own words), `unevaluated_conditions: list[str] = []`. Note it has extra fields beyond the ask (reference_id, expected_pattern, selected_pattern, alignment).

- `solution/schema.py:218-266` — **SolutionEstimate** exact fields: `recommended_pattern: str`, `recommended_implementation: str = ""`, `candidate_implementations: list[str] = []`, `task_automation: list[TaskAutomationEstimate] = []`, `overall_automation: RangeEstimate`, `performance: list[PerformanceMetric] = []`, `reference_comparison: Optional[ReferenceComparison] = None`, `integration_complexity: EffortBand`, `engineering_effort: EffortBand`, `engineering_hours: RangeEstimate`, `hitl_requirements: dict[str, HitlMode] = {}`, `risks_and_mitigations: list[dict[str,str]] = []`, `key_uncertainties: list[str] = []`, `fit_explanations: list[str] = []`, `needs_more_information: list[str] = []`, `effort_basis: str = ""`, `integration_basis: str = ""`, `integration_complexity_reported: Optional[EffortBand] = None`, `engineering_cost: Optional[RangeEstimate] = None`, `capability_validation: Optional[dict] = None`, `risk_controls: list[dict] = []`, `operating_cost_inputs: Optional[OperatingCostInputs] = None`, `assessment_confidence: str = "medium"`, `confidence_score: float = 0.0`, `confidence_notes: list[str] = []`, `time_reconciliation: Optional[dict] = None`, `provenance_warnings: list[str] = []`, `compliance_gap: bool = False`, `compliance_statement: str = ""`, `compliance_exclusions: list[dict] = []`, `compliance_verdicts: list[dict] = []`.

- `solution/schema.py:205-216` — **OperatingCostInputs**: `inference_price_per_unit: Optional[RangeEstimate] = None`, `inference_basis: str = ""`, `human_review_share: Optional[float] = None`, `absent_components: list[str] = []`. (Not in the ask but referenced via SolutionEstimate.operating_cost_inputs.)

- `solution/estimator.py:399-401` — **risk_controls** dict shape: each dict is produced by `c.model_dump()` on a `RiskControl`, which has fields `category` (RiskCategory enum), `risk: str`, `controls: list[str]`, `trigger: str = ""`, `implementation_controls: list[str]`, `implementation_id: str`. As JSON these are dict keys `category` (enum value string), `risk`, `controls`, `trigger`, `implementation_controls`, `implementation_id`. `risks_and_mitigations` dicts are `{"risk": c.risk, "mitigation": "; ".join(c.controls)}` — only two keys.

- `solution/schema.py:83-104` — **ComplianceClaim** (the base class used in Compatibility), status: `SUPPORTED / UNSUPPORTED / UNKNOWN`, `satisfies()` returns True only if SUPPORTED with evidence_id. `ComplianceStatus` enum at :70-80.

- `solution/estimator.py:410-413` + `solution/ranking.py:96-107` + `lib/compliance.py:182-198` — **compliance_verdicts** dict shape: `v.model_dump(mode="json")` of `ComplianceVerdict` (a BaseModel) with fields `implementation_id: str`, `standard: str`, `status: ClaimStatus` (enum), `components: list[ComponentVerdict] = []`, `reason: str = ""`. `ComponentVerdict` (lib/compliance.py:174) = `component_id: str`, `status: ClaimStatus`, `evidence_ids: list[str] = []`, `reason: str = ""`, `scope: Optional[str] = ""`.

- `solution/estimator.py:409` + `solution/ranking.py:237-243` — **compliance_exclusions** dict shape: `e.model_dump()` of `ExcludedCandidate` = `pattern_id: str`, `implementation_id: str`, `standard: str`, `status: str`, `reason: str`.

- `solution/schema.py:309-344` — **AlternativeComparison** (`comparison` field of `Alternative`): `approach: str` (registry: pattern.architecture), `strengths: list[str] = []`, `limitations: list[str] = []`, `implementation_complexity: Optional[EffortBand] = None`, `implementation_complexity_basis: str = ""`, `expected_automation: list[PerformanceMetric] = []`, `automation_basis: str = ""`, `human_involvement: HumanInvolvement = NOT_DECLARED`, `human_involvement_basis: list[str] = []`, `risks: list[str] = []`, `when_preferable: list[str] = []`.

- `solution/schema.py:328-345` — **Alternative**: `id: str`, `name: str`, `source: AlternativeSource = REGISTRY`, `pattern_id: str = ""`, `implementation_id: str = ""`, `implementation_kind: Optional[ImplementationKind] = None`, `difference_kind: DifferenceKind = ARCHITECTURE`, `difference_from_primary: str = ""`, `comparison: AlternativeComparison`, `uncertainties: list[str] = []`, `explanation: str = ""`, `ranking_score: Optional[float] = None`. `DifferenceKind` enum (:290): ARCHITECTURE, IMPLEMENTATION_MODEL, NO_AI_BASELINE. `AlternativeSource` (:280): REGISTRY, CURRENT_PROCESS.

- `solution/schema.py:347-356` — **RejectedAlternative** (the `rejected` entry): `pattern_id: str = ""`, `implementation_id: str = ""`, `reason: str` (required).

- `solution/schema.py:358-373` — **AlternativesResult**: `alternatives: list[Alternative] = []`, `statement: str = ""`, `rejected: list[RejectedAlternative] = []`, `categories_not_in_registry: list[str] = []`, `ordering_basis: str = ""`, `llm_guard_notes: list[str] = []`, `is_recommendation: bool = False`, `economics_included: bool = False`.

- `calc/models.py:209-239` — **Score** exact fields: `key: str`, `label: str`, `computable: bool = True`, `value: Optional[float] = None` (headline from input midpoints), `bounds: Optional[RangeEstimate] = None` (0-100 from input bounds), `band: str = ""`, `sub_scores: list[SubScore] = []`, `missing_inputs: list[str] = []`, `flags: list[str] = []`, `note: str = ""`, `bounds_type: BoundsType = UNAVAILABLE`, `inputs_varied: list[str] = []`, `inputs_held_fixed: list[str] = []`, `calibration_version: int = 1`. Note it has `note` not `presentable_as_strong`; `SubScore` (:183-195) = `key, label, value: float (0-100), weight: float, basis: str, provenance: Provenance = DERIVED, note: str = ""`, with `contribution` property. `BoundsType` enum (:198-206) = NUMERIC_INPUT_ENVELOPE, SCENARIO_ENVELOPE, UNAVAILABLE. `band_for(value)` (:244-248) returns high/moderate-high/moderate/low/very low from thresholds 80/60/40/20.

- `calc/driver_ranking.py:90-100` — **ScoreBundle** exact fields: `economic: Score`, `feasibility: Score`, `risk: Score`, `composite: Score`, `result: EconomicResult`, `confidence: Optional[dict] = None` (this is set via `conf.model_dump(mode="json")` so it's a JSON dict).

- `calc/economic_score.py:91-113` — economic `Score` is built with `bounds` as a RangeEstimate, `band`, `sub_scores` (payback weight 0.60, benefit_cost weight 0.40). `calc/feasibility_score.py:78-108` — feasibility Score with `band`, sub_scores (data_readiness 0.45, achievability 0.30, integration 0.25). `calc/risk_score.py:193-206` — risk Score with `band`, sub_scores (raw_error, failure_probability, failure_impact, reliability_gap optional), and a compliance blocker flag.

- `calc/engine.py:77-113` — **EconomicResult** exact fields: `sector: Sector`, `labor_realization: LaborRealization`, `realization_statement: str`, `labor_consistency: labor.LaborConsistency`, `baseline_basis: str`, `time_reconciliation: Optional[dict] = None`, `current_annual_cost: CostBreakdown`, `current_annual_total: RangeEstimate`, `tasks: list[TaskEconomics] = []`, `ai_operating: CostBreakdown`, `ai_operating_total: RangeEstimate`, `freed_capacity_value: Optional[RangeEstimate] = None`, `implementation: CostBreakdown`, `implementation_total: RangeEstimate`, `first_year: FirstYearEconomics`, `unit_economics: UnitEconomics`, `benchmark: BenchmarkCrossCheck`, `quality_comparison: Optional[dict] = None`, `reliability: Optional[dict] = None`, `labor_rate_geography: Optional[str] = None`, `inference_pricing_ids: list[str] = []`, `inference_lineage: list[str] = []`, `provenance_lineage: dict[str, list[str]] = {}`, `warnings: list[str] = []`, `absent_components: list[str] = []`. `realization_statement` is a plain string field; `labor_realization` is the `LaborRealization` enum.

- `calc/ai_state.py:69-72` — **LaborRealization** enum values: COST_ELIMINATED="cost_eliminated", CAPACITY_RETAINED="capacity_retained". `TaskEconomics` (:75-86): `task: str`, `hitl: HitlMode`, `workload_share: float`, `current_cost: RangeEstimate`, `automation: RangeEstimate` (fraction 0-1), `residual_work_fraction: RangeEstimate`, `modelled_residual_labor: RangeEstimate`, `realized_residual_labor: RangeEstimate`, `human_review_cost: Optional[RangeEstimate] = None`, `freed_capacity_value: RangeEstimate`, `mechanism: str = ""`.

- `calc/lifecycle.py:31-39` — **FirstYearEconomics**: `implementation_cost: RangeEstimate`, `ai_annual_operating_cost: RangeEstimate`, `first_year_ai_cost: RangeEstimate`, `annual_cost_savings: RangeEstimate`, `first_year_net_benefit: RangeEstimate`, `monthly_net_benefit: RangeEstimate`, `payback_months: Optional[RangeEstimate] = None`, `payback_statement: str = ""`. `:22-28` — **UnitEconomics**: `current_unit_cost`, `ai_unit_cost`, `first_year_unit_cost`, `current_valid_output`, `ai_valid_output` (all Optional[RangeEstimate]), `note: str = ""`.

- `calc/labor.py:95-116` — **LaborConsistency**: `task_based: Optional[RangeEstimate] = None`, `workforce_based: Optional[RangeEstimate] = None`, `comparable: bool = False`, `divergence: Optional[float] = None`, `status: BaselineStatus = UNRESOLVED`, `primary: Optional[RangeEstimate] = None`, `secondary: Optional[RangeEstimate] = None`, `primary_basis: str = ""`, `verdict: str = ""`; property `needs_more_information`.

- `calc/models.py:140-177` — **CostLine**: `key: str`, `label: str`, `amount: Optional[RangeEstimate] = None`, `status: LineStatus = KNOWN`, `note: str = ""`; `absent()` classmethod sets amount=None, status=ABSENT. **CostBreakdown**: `label: str`, `lines: list[CostLine] = []`, with `known_lines`, `absent_lines` properties and `total()` summing KNOWN lines. **LineStatus** enum: KNOWN/ABSENT. Note the score's `presentable_as_strong` from the query does NOT exist — it's on `sanity` (in `calc/economic_sanity.py`), accessed in economic_score.py as `sanity.presentable_as_strong`, not on the `Score` itself.

### Recommendation
`report/assemble.py` should consume: `ScoreBundle` (via `compute_scores` in calc/driver_ranking.py) for economic/feasibility/risk scores; `EconomicResult` (via `run` in calc/engine.py) for economics/realization/first-year; `AlternativesResult` (in solution/alternatives.py); `SolutionEstimate`; `AssessmentState`. Re-exported aliases: `solution.schema` re-exports Provenance/RangeEstimate/Sector/EffortBand from `schemas.assessment_state` (same class identity).

### Not covered / Uncertain
- I did NOT read `calc/economic_sanity.py` (the `sanity` object with `flags`, `level`, `outcome`, `presentable_as_strong` — its exact field set is inferred from usage as `sanity.flags`, `sanity.level.value`, `sanity.outcome.value`, `sanity.presentable_as_strong`), nor `calc/benchmark_check.py` (BenchmarkCrossCheck) nor `calc/assessment_confidence.py` (the `confidence` dict produced by `assess`), nor `calc/composite.py` (composite_score), nor `solution/alternatives.py` (how AlternativesResult is built). The `difference_kind`/`difference_from_primary` fields in `Alternative` and `comparison` object are confirmed at the schema level, but I did not verify runtime fill from `alternatives.py`. The `confidence` dict in `ScoreBundle` and `AssetConfidence`'s exact key set is only known as `model_dump(mode="json")` — key names unverified.

## Compressed Report (what the parent actually received)

<HYDRATION_REPORT>
I'll map these objects precisely. Let me start by locating the relevant files and reading their schemas.Let me read the three key files: solution/schema.py, schemas/assessment_state.py, and calc/ai_state.py.Now let me examine the economic/feasibility/risk score files and find the `Score`, `ScoreBundle`, and `EconomicResult` definitions.Now I need `ScoreBundle`, `EconomicResult`, and `FirstYearEconomics`. Let me locate them.Let me read the `ScoreBundle`, `EconomicResult`, and `FirstYearEconomics` definitions, plus the engine's realization_statement context.Let me check how `risk_controls`, `compliance_verdicts`, and `compliance_exclusions` dicts are structured, plus the `LaborConsistency` referenced in EconomicResult.Let me read the exact dict shapes for `risk_controls`, `compliance_verdicts`/`compliance_exclusions` construction, and `LaborConsistency`.Let me find the `RiskControl`, `ComplianceVerdict`, and `ComplianceExclusion` class shapes.Let me read `RiskControl`, `ComplianceVerdict`, and search for the compliance exclusion object.Let me read the `ExcludedCandidate`, `ComponentVerdict`, and `ClaimStatus` shapes to fully describe the exclusion/verdict dict structure.The dedup blocked reads of different line ranges. Let me re-read the specific ranges I need.## Exploration Report

**Confidence**: HIGH - I read the three main schema files in full and all score/economic/labor definition sites. Partial gap: none critical.

**Files Examined**: solution/schema.py (full, 373 lines), schemas/assessment_state.py (full, 427 lines), calc/ai_state.py (full, 287 lines), calc/models.py (full 253 lines), calc/economic_score.py, calc/feasibility_score.py, calc/risk_score.py, calc/lifecycle.py (lines 1-120), calc/engine.py (lines 60-130), calc/driver_ranking.py (lines 80-130), calc/labor.py (lines 90-120), solution/estimator.py (lines 360-425), solution/ranking.py (lines 90-130, 237-250), solution/risks.py (lines 25-60), lib/compliance.py (lines 160-215).

**Key Tool Path**: listDirectory → readFile (schemas) → search (ScoreBundle/EconomicResult/FirstYearEconomics) → targeted reads of dict-shape constructors.

### Findings

- `schemas/assessment_state.py:273-347` — **AssessmentState** exact fields: `sector: Sector` (required, no default), `problem: str = ""`, `geography: Optional[str] = None`, `process: Optional[str] = None`, `monthly_volume: Optional[RangeEstimate] = None`, `avg_time_per_unit_minutes: Optional[RangeEstimate] = None`, `current_headcount: Optional[RangeEstimate] = None`, `worker_role: Optional[str] = None` (raw user words), `worker_role_canonical: Optional[ProcessRole] = None`, `fully_loaded_annual_cost: Optional[RangeEstimate] = None`, `fraction_time_on_process: Optional[RangeEstimate] = None` (0-1). Other current-cost components (all Optional[RangeEstimate]): `annual_tooling_cost`, `monthly_tooling_cost`, `error_rate` (fraction 0-1), `rework_time_per_error_minutes`, `annual_rework_cost`, `annual_other_direct_cost`, plus `other_direct_cost_description: Optional[str]`. Quality: `current_quality_metric: Optional[CurrentQualityMetric]`, `current_quality_value: Optional[RangeEstimate]`. Feasibility: `required_accuracy: Optional[RangeEstimate]` (0-1), `existing_data: Optional[str]`, `data_readiness: Optional[DataReadiness]`, `current_tools: list[str] = []`, `integration_complexity: Optional[EffortBand]`. `risk: RiskInputs = default_factory`. `process_stages: list[ProcessStage] = []`. Bookkeeping: `provenance: dict[str, Provenance] = {}`, `turn_count: int = 0`, `complete: bool = False`, `status: InterviewStatus = INTERVIEWING`, `field_resolution: dict[str, FieldMeta] = {}`. Methods: `currency` is a PROPERTY (not field) returning Optional[str] derived from `GEOGRAPHY_CURRENCY` via geography (e.g. india/in→INR, us/usa/united states→USD); else None. `get_value(field)` resolves dotted paths (reads nested via getattr); `get_tag(field)` returns `self.provenance.get(field)`; `set_value(field, value, provenance)` writes dotted paths and coerces scalars/dicts into RangeEstimate for range-typed fields. Note: `field_resolution` is a dict of `FieldMeta`, and field are NOT flat — `current_tools`, `process_stages` etc. are list fields. `model_config = {"validate_assignment": True}`.

- `schemas/assessment_state.py:16-31` — **Provenance** enum values: USER_PROVIDED="user_provided", SOURCED="sourced", ESTIMATED="estimated", ASSUMED="assumed", DERIVED="derived".

- `schemas/assessment_state.py:34-36` — **Sector** enum values: CUSTOMER_SUPPORT="customer_support", DOCUMENT_PROCESSING="document_processing" (only those two).

- `schemas/assessment_state.py:52-69` — **RangeEstimate**: `min: float`, `max: float`, `confidence: Literal["low","medium","high"] = "medium"`, `provenance: Provenance = ESTIMATED`, `source: str = ""` (rendered citation), `source_id: Optional[str] = None` (stable evidence id).

- `schemas/assessment_state.py:72-84` — **DataReadiness** enum: NONE="none", MINIMAL="minimal", PARTIAL="partial", GOOD="good", EXCELLENT="excellent". `:86-93` — **ImpactSeverity** enum: NEGLIGIBLE, MINOR, MODERATE, MAJOR, SEVERE. `:166-169` — **EffortBand** enum: SMALL="small", MEDIUM="medium", LARGE="large".

- `schemas/assessment_state.py:95-115` — **ProcessRole** enum: CUSTOMER_SUPPORT_AGENT, CUSTOMER_SUPPORT_SPECIALIST, ACCOUNTS_PAYABLE_CLERK. `:118-133` — CurrentQualityMetric enum (FIRST_CONTACT_RESOLUTION, ESCALATION_RATE, REWORK_RATE, EXCEPTION_RATE, FIRST_PASS_YIELD, STRAIGHT_THROUGH_RATE).

- `schemas/assessment_state.py:203-225` — **RiskInputs** (`model_config = {"validate_assignment": True}`): `failure_impact: Optional[str] = None` (free text), `failure_impact_severity: Optional[ImpactSeverity] = None`, `compliance_exposure: Optional[list[str]] = None` (canonical keys), `compliance_requirements: list[ComplianceRequirement] = []`. `failure_probability` and `reliability_gap` are NOT fields — derived elsewhere.

- `schemas/assessment_state.py:151-163` — **ComplianceRequirement**: `standard: str`, `stated_as: str = ""`, `hard_requirement: bool = True`.

- `schemas/assessment_state.py:186-200` — **FieldResolution** enum (MISSING, RESOLVED, AMBIGUOUS, LOW_CONFIDENCE, CONTRADICTORY, NEEDS_DETAIL) and **FieldMeta**: `status: FieldResolution = MISSING`, `attempts: int = 0`, `reason: Optional[str] = None`.

- `solution/schema.py:171-183` — **TaskAutomationEstimate**: `task: str`, `capability: Capability` (enum, required), `architecture: str`, `benchmark_basis: str` (LLM prose only), `estimate: RangeEstimate`, `hitl: HitlMode = AI_ASSISTED`, `workload_share: float = 0.0` (derived by calc, never LLM-supplied), `workload_share_provenance: Provenance = DERIVED`, `handling_time_minutes: Optional[RangeEstimate] = None`, `benchmark_anchor: Optional[str] = None`, `divergence_note: str = ""`.

- `solution/schema.py:38-44` — **HitlMode** enum: AUTONOMOUS="autonomous", AI_ASSISTED="ai_assisted", HUMAN_REVIEW="human_review", HUMAN_ONLY="human_only", ESCALATION="escalation".

- `solution/schema.py:26-30` — **PerformanceMetric**: `metric: str`, `estimate: RangeEstimate`.

- `solution/schema.py:186-202` — **ReferenceComparison**: `reference_id: str`, `expected_pattern: str`, `selected_pattern: str`, `match: bool`, `alignment: float` (0-1, entered ranking), `deviation_reason: str = ""`, `active_deviations: list[str] = []` (conditions that fired, in reference's own words), `unevaluated_conditions: list[str] = []`. Note it has extra fields beyond the ask (reference_id, expected_pattern, selected_pattern, alignment).

- `solution/schema.py:218-266` — **SolutionEstimate** exact fields: `recommended_pattern: str`, `recommended_implementation: str = ""`, `candidate_implementations: list[str] = []`, `task_automation: list[TaskAutomationEstimate] = []`, `overall_automation: RangeEstimate`, `performance: list[PerformanceMetric] = []`, `reference_comparison: Optional[ReferenceComparison] = None`, `integration_complexity: EffortBand`, `engineering_effort: EffortBand`, `engineering_hours: RangeEstimate`, `hitl_requirements: dict[str, HitlMode] = {}`, `risks_and_mitigations: list[dict[str,str]] = []`, `key_uncertainties: list[str] = []`, `fit_explanations: list[str] = []`, `needs_more_information: list[str] = []`, `effort_basis: str = ""`, `integration_basis: str = ""`, `integration_complexity_reported: Optional[EffortBand] = None`, `engineering_cost: Optional[RangeEstimate] = None`, `capability_validation: Optional[dict] = None`, `risk_controls: list[dict] = []`, `operating_cost_inputs: Optional[OperatingCostInputs] = None`, `assessment_confidence: str = "medium"`, `confidence_score: float = 0.0`, `confidence_notes: list[str] = []`, `time_reconciliation: Optional[dict] = None`, `provenance_warnings: list[str] = []`, `compliance_gap: bool = False`, `compliance_statement: str = ""`, `compliance_exclusions: list[dict] = []`, `compliance_verdicts: list[dict] = []`.

- `solution/schema.py:205-216` — **OperatingCostInputs**: `inference_price_per_unit: Optional[RangeEstimate] = None`, `inference_basis: str = ""`, `human_review_share: Optional[float] = None`, `absent_components: list[str] = []`. (Not in the ask but referenced via SolutionEstimate.operating_cost_inputs.)

- `solution/estimator.py:399-401` — **risk_controls** dict shape: each dict is produced by `c.model_dump()` on a `RiskControl`, which has fields `category` (RiskCategory enum), `risk: str`, `controls: list[str]`, `trigger: str = ""`, `implementation_controls: list[str]`, `implementation_id: str`. As JSON these are dict keys `category` (enum value string), `risk`, `controls`, `trigger`, `implementation_controls`, `implementation_id`. `risks_and_mitigations` dicts are `{"risk": c.risk, "mitigation": "; ".join(c.controls)}` — only two keys.

- `solution/schema.py:83-104` — **ComplianceClaim** (the base class used in Compatibility), status: `SUPPORTED / UNSUPPORTED / UNKNOWN`, `satisfies()` returns True only if SUPPORTED with evidence_id. `ComplianceStatus` enum at :70-80.

- `solution/estimator.py:410-413` + `solution/ranking.py:96-107` + `lib/compliance.py:182-198` — **compliance_verdicts** dict shape: `v.model_dump(mode="json")` of `ComplianceVerdict` (a BaseModel) with fields `implementation_id: str`, `standard: str`, `status: ClaimStatus` (enum), `components: list[ComponentVerdict] = []`, `reason: str = ""`. `ComponentVerdict` (lib/compliance.py:174) = `component_id: str`, `status: ClaimStatus`, `evidence_ids: list[str] = []`, `reason: str = ""`, `scope: Optional[str] = ""`.

- `solution/estimator.py:409` + `solution/ranking.py:237-243` — **compliance_exclusions** dict shape: `e.model_dump()` of `ExcludedCandidate` = `pattern_id: str`, `implementation_id: str`, `standard: str`, `status: str`, `reason: str`.

- `solution/schema.py:309-344` — **AlternativeComparison** (`comparison` field of `Alternative`): `approach: str` (registry: pattern.architecture), `strengths: list[str] = []`, `limitations: list[str] = []`, `implementation_complexity: Optional[EffortBand] = None`, `implementation_complexity_basis: str = ""`, `expected_automation: list[PerformanceMetric] = []`, `automation_basis: str = ""`, `human_involvement: HumanInvolvement = NOT_DECLARED`, `human_involvement_basis: list[str] = []`, `risks: list[str] = []`, `when_preferable: list[str] = []`.

- `solution/schema.py:328-345` — **Alternative**: `id: str`, `name: str`, `source: AlternativeSource = REGISTRY`, `pattern_id: str = ""`, `implementation_id: str = ""`, `implementation_kind: Optional[ImplementationKind] = None`, `difference_kind: DifferenceKind = ARCHITECTURE`, `difference_from_primary: str = ""`, `comparison: AlternativeComparison`, `uncertainties: list[str] = []`, `explanation: str = ""`, `ranking_score: Optional[float] = None`. `DifferenceKind` enum (:290): ARCHITECTURE, IMPLEMENTATION_MODEL, NO_AI_BASELINE. `AlternativeSource` (:280): REGISTRY, CURRENT_PROCESS.

- `solution/schema.py:347-356` — **RejectedAlternative** (the `rejected` entry): `pattern_id: str = ""`, `implementation_id: str = ""`, `reason: str` (required).

- `solution/schema.py:358-373` — **AlternativesResult**: `alternatives: list[Alternative] = []`, `statement: str = ""`, `rejected: list[RejectedAlternative] = []`, `categories_not_in_registry: list[str] = []`, `ordering_basis: str = ""`, `llm_guard_notes: list[str] = []`, `is_recommendation: bool = False`, `economics_included: bool = False`.

- `calc/models.py:209-239` — **Score** exact fields: `key: str`, `label: str`, `computable: bool = True`, `value: Optional[float] = None` (headline from input midpoints), `bounds: Optional[RangeEstimate] = None` (0-100 from input bounds), `band: str = ""`, `sub_scores: list[SubScore] = []`, `missing_inputs: list[str] = []`, `flags: list[str] = []`, `note: str = ""`, `bounds_type: BoundsType = UNAVAILABLE`, `inputs_varied: list[str] = []`, `inputs_held_fixed: list[str] = []`, `calibration_version: int = 1`. Note it has `note` not `presentable_as_strong`; `SubScore` (:183-195) = `key, label, value: float (0-100), weight: float, basis: str, provenance: Provenance = DERIVED, note: str = ""`, with `contribution` property. `BoundsType` enum (:198-206) = NUMERIC_INPUT_ENVELOPE, SCENARIO_ENVELOPE, UNAVAILABLE. `band_for(value)` (:244-248) returns high/moderate-high/moderate/low/very low from thresholds 80/60/40/20.

- `calc/driver_ranking.py:90-100` — **ScoreBundle** exact fields: `economic: Score`, `feasibility: Score`, `risk: Score`, `composite: Score`, `result: EconomicResult`, `confidence: Optional[dict] = None` (this is set via `conf.model_dump(mode="json")` so it's a JSON dict).

- `calc/economic_score.py:91-113` — economic `Score` is built with `bounds` as a RangeEstimate, `band`, `sub_scores` (payback weight 0.60, benefit_cost weight 0.40). `calc/feasibility_score.py:78-108` — feasibility Score with `band`, sub_scores (data_readiness 0.45, achievability 0.30, integration 0.25). `calc/risk_score.py:193-206` — risk Score with `band`, sub_scores (raw_error, failure_probability, failure_impact, reliability_gap optional), and a compliance blocker flag.

- `calc/engine.py:77-113` — **EconomicResult** exact fields: `sector: Sector`, `labor_realization: LaborRealization`, `realization_statement: str`, `labor_consistency: labor.LaborConsistency`, `baseline_basis: str`, `time_reconciliation: Optional[dict] = None`, `current_annual_cost: CostBreakdown`, `current_annual_total: RangeEstimate`, `tasks: list[TaskEconomics] = []`, `ai_operating: CostBreakdown`, `ai_operating_total: RangeEstimate`, `freed_capacity_value: Optional[RangeEstimate] = None`, `implementation: CostBreakdown`, `implementation_total: RangeEstimate`, `first_year: FirstYearEconomics`, `unit_economics: UnitEconomics`, `benchmark: BenchmarkCrossCheck`, `quality_comparison: Optional[dict] = None`, `reliability: Optional[dict] = None`, `labor_rate_geography: Optional[str] = None`, `inference_pricing_ids: list[str] = []`, `inference_lineage: list[str] = []`, `provenance_lineage: dict[str, list[str]] = {}`, `warnings: list[str] = []`, `absent_components: list[str] = []`. `realization_statement` is a plain string field; `labor_realization` is the `LaborRealization` enum.

- `calc/ai_state.py:69-72` — **LaborRealization** enum values: COST_ELIMINATED="cost_eliminated", CAPACITY_RETAINED="capacity_retained". `TaskEconomics` (:75-86): `task: str`, `hitl: HitlMode`, `workload_share: float`, `current_cost: RangeEstimate`, `automation: RangeEstimate` (fraction 0-1), `residual_work_fraction: RangeEstimate`, `modelled_residual_labor: RangeEstimate`, `realized_residual_labor: RangeEstimate`, `human_review_cost: Optional[RangeEstimate] = None`, `freed_capacity_value: RangeEstimate`, `mechanism: str = ""`.

- `calc/lifecycle.py:31-39` — **FirstYearEconomics**: `implementation_cost: RangeEstimate`, `ai_annual_operating_cost: RangeEstimate`, `first_year_ai_cost: RangeEstimate`, `annual_cost_savings: RangeEstimate`, `first_year_net_benefit: RangeEstimate`, `monthly_net_benefit: RangeEstimate`, `payback_months: Optional[RangeEstimate] = None`, `payback_statement: str = ""`. `:22-28` — **UnitEconomics**: `current_unit_cost`, `ai_unit_cost`, `first_year_unit_cost`, `current_valid_output`, `ai_valid_output` (all Optional[RangeEstimate]), `note: str = ""`.

- `calc/labor.py:95-116` — **LaborConsistency**: `task_based: Optional[RangeEstimate] = None`, `workforce_based: Optional[RangeEstimate] = None`, `comparable: bool = False`, `divergence: Optional[float] = None`, `status: BaselineStatus = UNRESOLVED`, `primary: Optional[RangeEstimate] = None`, `secondary: Optional[RangeEstimate] = None`, `primary_basis: str = ""`, `verdict: str = ""`; property `needs_more_information`.

- `calc/models.py:140-177` — **CostLine**: `key: str`, `label: str`, `amount: Optional[RangeEstimate] = None`, `status: LineStatus = KNOWN`, `note: str = ""`; `absent()` classmethod sets amount=None, status=ABSENT. **CostBreakdown**: `label: str`, `lines: list[CostLine] = []`, with `known_lines`, `absent_lines` properties and `total()` summing KNOWN lines. **LineStatus** enum: KNOWN/ABSENT. Note the score's `presentable_as_strong` from the query does NOT exist — it's on `sanity` (in `calc/economic_sanity.py`), accessed in economic_score.py as `sanity.presentable_as_strong`, not on the `Score` itself.

### Recommendation
`report/assemble.py` should consume: `ScoreBundle` (via `compute_scores` in calc/driver_ranking.py) for economic/feasibility/risk scores; `EconomicResult` (via `run` in calc/engine.py) for economics/realization/first-year; `AlternativesResult` (in solution/alternatives.py); `SolutionEstimate`; `AssessmentState`. Re-exported aliases: `solution.schema` re-exports Provenance/RangeEstimate/Sector/EffortBand from `schemas.assessment_state` (same class identity).

### Not covered / Uncertain
- I did NOT read `calc/economic_sanity.py` (the `sanity` object with `flags`, `level`, `outcome`, `presentable_as_strong` — its exact field set is inferred from usage as `sanity.flags`, `sanity.level.value`, `sanity.outcome.value`, `sanity.presentable_as_strong`), nor `calc/benchmark_check.py` (BenchmarkCrossCheck) nor `calc/assessment_confidence.py` (the `confidence` dict produced by `assess`), nor `calc/composite.py` (composite_score), nor `solution/alternatives.py` (how AlternativesResult is built). The `difference_kind`/`difference_from_primary` fields in `Alternative` and `comparison` object are confirmed at the schema level, but I did not verify runtime fill from `alternatives.py`. The `confidence` dict in `ScoreBundle` and `AssetConfidence`'s exact key set is only known as `model_dump(mode="json")` — key names unverified.

---
[Exploration: Explore (deepseek-v4-flash) | 64083ms | 11 turns | 36 tools | 84583in/7889out tokens | status: completed]
</HYDRATION_REPORT>

CRITICAL: Write your final answer DIRECTLY from the findings above. Do NOT re-page files the sub-agent already covered (no whole-file skeleton/symbols/read on those paths). To confirm one specific cited line, a narrow sb_read_code range (<=40 lines) is allowed. Files the report did NOT cover stay fully readable — locate them with superbrain_listDirectory / superbrain_search instead of guessing.

---
**Explore coverage (harness):** opened 15 file(s) over 11 turn(s). listed but not opened: .. Treat any subsystem this report does not explicitly cover as UNVERIFIED, not absent - confirm with a direct read or say what you could not verify.
